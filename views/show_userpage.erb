<!DOCTYPE html>
<html>
<head>
<style>
#header {
    background-color:black;
    color:white;
    text-align:center;
    padding:5px;
}
#nav {
    line-height:30px;
    background-color:#eeeeee;
    height:300px;
    width:100px;
    float:left;
    padding:5px;	      
}
#section {
    width:350px;
    float:left;
    padding:10px;	 	 
}
#footer {
    background-color:black;
    color:white;
    clear:both;
    text-align:center;
   padding:5px;	 	 
}
</style>
</head>

<body>

<div id="header">
	<h1> <%= @owner_name%> 's Page</h1>
</div>

<div id="nav">
Picture<br>
<form action = "/profile" method="GET">
<input type="submit" value = "Profile" style="float: blue;"/>
</form>
<br>
Follower number<br>
Followed number<br>
</div>

<div id="section">
	<% if @log_status %> 
		<form action='/logout' method="GET">
			<input type="submit" value = "Logout" style="float: right;"/>
		</form>
		<%puts @followed_flag%>
		<% if @followed_flag %>
			<form action='/unfollow?follower_id=<%=@user_id%>&followee_id=<%=@owner_id%>' method="POST">
				<input type="submit" value = "unfollow" style="float: right;"/>
			</form>
		<%else%>
			<form action='/follow?follower_id=<%=@user_id%>&followee_id=<%=@owner_id%>' method="POST">
				<input type="submit" value = "follow" style="float: right;"/>
			</form>
		<%end%>

	<%else%>
		<form action='/login' method="GET">
			<input type="submit" value = "Login" style="float: right;"/>
		</form>
		
		<form action='/register' method="GET">
			<input type="submit" value = "register" style="float: right;"/>
		</form>
	<%end%>


	<form action="/mypage" method="GET">
		<input type="submit" value = "mypage" style="float: right;"/>      
	</form>

	<h2><%= @owner_name%>'s Tweets </h2>
	<% @tweets.each do |tweet| %>
 	<% @user_id= tweet.user_id %>
 	<% user = User.find_by(id: @user_id)%>
 	<% user_name = user.user_name%>
	<p><a href="/user_page?user_id=<%= @user_id %>"> <%= user_name %> </a>
	<% diff = (Time.now - tweet.created_at).to_i %>
 		<% if diff < 60 %>	
			<%= diff %>s ago </p>
		<% elsif diff >= 60 && diff < 3600 %>
			<%= (diff / 60).to_i %>m ago</p>
		<% elsif diff >= 3600 && diff < 86400 %>
			<%= (diff / 3600).to_i %>h ago</p>
		<% else  %>
			<%= (diff / 86400).to_i %>d ago</p>
		<% end %>
	<%= tweet.text%>	
	<br/>
<% end %>
</div>

<div id="footer">
Copyright © nanotwitter group
</div>

</body>
</html>


